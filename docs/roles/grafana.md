**grafana.md — Grafana Role**

Runs Grafana using Docker.

Tasks:
    - Starts a Grafana container from the official image
    - Exposes Grafana on port 3000
    - Ensures the container automatically restarts
